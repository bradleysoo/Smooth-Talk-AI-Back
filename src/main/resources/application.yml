server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smoothtalkai  # ← DB 이름 맞추기
    username: smoothtalkai                              # ← POSTGRES_USER
    password: smoothtalkai123!                         # ← POSTGRES_PASSWORD
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
    open-in-view: false
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 318978225816-2neqm94qo4a3oe0n6d02h4lhbmq1d25p.apps.googleusercontent.com
            client-secret: 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
            redirect-uri: "http://localhost:8080/login/oauth2/code/google"
            scope:
              - openid
              - profile
              - email
        provider:
          google:
            issuer-uri: https://accounts.google.com

logging:
  level:
    org.springframework.security: INFO
    com.smoothTalkAI.backend: DEBUG

jwt:
  secret: ${JWT_SECRET:this_is_a_dev_jwt_secret_key_that_is_long_enough_123456}
  access-token-validity-in-sec: 3600

redis:
  host: localhost
  port: 6379

app:
  python:
    analyze-url: ${PYTHON_ANALYZE_URL:http://localhost:5000/api/analyze}
  frontend:
    url: ${FRONTEND_URL:http://localhost:5173}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
  oauth2:
    success-redirect: ${FRONTEND_URL:http://localhost:5173}
    failure-redirect: ${FRONTEND_URL:http://localhost:5173}

kakao:
  pay:
    api-url: https://open-api.kakaopay.com/online/v1/payment
    cid: TC0ONETIME
    secret-key: ${KAKAO_PAY_SECRET_KEY:DEV1929B0BCDCCD4B1BD487720882772357FF748}

